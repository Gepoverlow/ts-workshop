// Test data
const data = [1, 'hello', -5, { a: 1 }, 10, 'world'];

describe('processData', () => {
  test('filters positive numbers and doubles them', () => {

    // Your test

  });

  test('filters strings and converts to uppercase', () => {

    // Your test

  });

  test('filters even numbers and turns them into strings', () => {

    // Your test

  });

  test('filters strings with a minimum length and capitalizes them', () => {

    // Your test

  });

  test('filters positive numbers in range and turns them into booleans', () => {

    // Your test

  });

  test('filter all strings with whitespace and replace that whitespace with "-"', () => {

    // Your test

  });

  test('stringifies all items', () => {

    // Your test

  });

  // ... 

});